# expect: 20
# Result type operations using stdlib Result
use stdlib.list.*

safeDivide(a: Int, b: Int) -> Result[Int, String] =
    if b == 0 { Err("division by zero") }
    else { Ok(a / b) }

main() = match safeDivide(100, 5) {
    Ok(n) -> n
    Err(_) -> 0
}
