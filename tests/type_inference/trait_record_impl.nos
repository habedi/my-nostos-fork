# expect: 50
# Trait implemented on record type

type Point = Point { x: Int, y: Int }

trait Measurable
    measure(self) -> Int
end

Point: Measurable
    measure(self) = self.x + self.y
end

main() = {
    p = Point(20, 30)
    p.measure()
}
