# expect: Some(10)
# Mapping over wrapped values (using Option since Result doesn't exist in stdlib)
double(x) = x * 2
main() = {
    # Use optMap to transform the inner value
    optMap(Some(5), double)
}
