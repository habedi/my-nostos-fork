# expect: 42
# Trait with default implementation
type Number = Number { n: Int }

trait Doubled
    doubled(self) -> Int
    quadrupled(self) -> Int = self.doubled() * 2
end

Number: Doubled
    doubled(self) = self.n * 2
end

main() = {
    num = Number(n: 21)
    num.doubled()
}
