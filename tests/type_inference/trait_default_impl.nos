# expect: 42
# Trait with default implementation
type IntNum = IntNum { n: Int }

trait Doubled
    doubled(self) -> Int
    quadrupled(self) -> Int = self.doubled() * 2
end

IntNum: Doubled
    doubled(self) = self.n * 2
end

main() = {
    num = IntNum(n: 21)
    num.doubled()
}
