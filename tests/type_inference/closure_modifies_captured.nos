# expect: 3
# Closure captures and modifies reactive record field
reactive Counter = { value: Int }

main() = {
    counter = Counter(0)
    inc = () => { counter.value = counter.value + 1; counter.value }
    inc()
    inc()
    inc()
}
