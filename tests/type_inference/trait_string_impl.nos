# expect: 10
# Trait implemented on String type

trait Lengthy
    len(self) -> Int
end

String: Lengthy
    len(self) = length(self)
end

doubleLen[T: Lengthy](x: T) -> Int = x.len() * 2

main() = {
    doubleLen("hello")
}
