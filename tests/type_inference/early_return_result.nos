# expect: 10
# Early return with stdlib Result type
use stdlib.list.*

parsePositive(n: Int) -> Result[Int, String] = {
    if n < 0 { return Err("negative") }
    if n == 0 { return Err("zero") }
    Ok(n)
}

main() = match parsePositive(10) {
    Ok(n) -> n
    Err(_) -> 0
}
