# expect: 5
# Pattern matching on generic variant
type Maybe[T] = | Just(T) | Nothing
getOrDefault(m, default) = match m {
    Just(x) -> x
    Nothing -> default
}
main() = {
    getOrDefault(Just(5), 0)
}
