# expect: 30
# Generic pair operations
type Pair[A, B] = Pair { first: A, second: B }

makePair[A, B](a: A, b: B) -> Pair[A, B] = Pair(first: a, second: b)

sumPair(p: Pair[Int, Int]) -> Int = p.first + p.second

main() = {
    p = makePair(10, 20)
    sumPair(p)
}
