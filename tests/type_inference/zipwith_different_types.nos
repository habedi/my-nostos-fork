# expect: ["1a", "2b", "3c"]
# ZipWith combining different types - stdlib uses (xs, ys, f) order
main() = {
    nums = [1, 2, 3]
    strs = ["a", "b", "c"]
    zipWith(nums, strs, (n, s) => show(n) ++ s)
}
