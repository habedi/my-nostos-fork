# expect: 35
# Record update syntax: Type(base, field: newValue)
type Point = { x: Int, y: Int }

main() = {
    p1 = Point(x: 10, y: 20)
    # Update x using record update syntax
    p2 = Point(p1, x: 15)
    p2.x + p2.y
}
