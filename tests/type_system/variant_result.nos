# expect: 100
# Result variant type (uses stdlib Result)

resultUnwrap(res, default) = match res {
    Ok(x) -> x
    Err(_) -> default
}

main() = {
    success: Result[Int, String] = Ok(100)
    resultUnwrap(success, 0)
}
