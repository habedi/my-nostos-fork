# expect: [2, 4]
# List filter with typed result

myFilter(p, xs) = match xs {
    [] -> []
    [h | t] -> if p(h) then [h | myFilter(p, t)] else myFilter(p, t)
}

main() = {
    nums: List[Int] = [1, 2, 3, 4, 5]
    evens: List[Int] = myFilter(x => x % 2 == 0, nums)
    evens
}
