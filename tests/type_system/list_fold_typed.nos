# expect: 15
# List fold with typed accumulator

myFold(f, acc, xs) = match xs {
    [] -> acc
    [h | t] -> myFold(f, f(acc, h), t)
}

main() = {
    nums: List[Int] = [1, 2, 3, 4, 5]
    total: Int = myFold((a, b) => a + b, 0, nums)
    total
}
