# expect: 5
# List length with typed result

myLen(xs) = match xs {
    [] -> 0
    [_ | t] -> 1 + myLen(t)
}

main() = {
    nums: List[Int] = [1, 2, 3, 4, 5]
    length: Int = myLen(nums)
    length
}
