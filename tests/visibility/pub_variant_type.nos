# expect: 15
# Test that public variant types are accessible from other modules

module StatusMod
    pub type Status = Success(Int) | Failure(String)
end

use StatusMod.*

main() = {
    ok = Success(15)
    err = Failure("fail")

    match ok {
        Success(n) -> n
        Failure(_) -> 0
    }
}
