# expect: Nothing
# Test jsonToType with unit variant
use stdlib.json.{jsonParse, jsonStringify}

type Maybe = Just(Int) | Nothing

main() = {
    json = jsonParse("{\"Nothing\": {}}")
    opt = jsonToType[Maybe](json)
    tagOf(opt)
}
