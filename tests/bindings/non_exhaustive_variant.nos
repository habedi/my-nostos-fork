# expect_error: non-exhaustive patterns
# Match on variant without covering all cases - now detected at compile time

type Color = Red | Green | Blue

main() = {
    c = Blue
    match c {
        Red -> 1
        Green -> 2
    }
}
