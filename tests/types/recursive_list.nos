# expect: 0
# Recursive type - custom list

type List[T] = Nil | Cons(T, List[T])

mySum(Nil) = 0
mySum(Cons(h, t)) = h + mySum(t)

main() = {
    lst = Cons(1, Cons(2, Cons(3, Cons(4, Nil))))
    result = mySum(lst)
    assert_eq(10, result)
    0
}
